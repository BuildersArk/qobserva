# Test dependencies (CI and local runs): pip install -r tests/requirements.txt
pytest
# Starlette 1.x TestClient prefers httpx2 (plain httpx is deprecated there)
httpx2
tomli; python_version < "3.11"
